<% if signed_in? %>

  <script type="text/javascript">
  <% if current_user.has_attribute?(:last_name)  %>
    <% show = "api/workers/worker.json.jbuilder" %>
    let key = "worker";
  <% else %>
    <% show = "api/companies/company.json.jbuilder" %>
    let key = "company";
  <% end %>

    window[key] = <%= render(
      show,
      user: current_user
    ).html_safe %>
    window.logo = "<%= image_url('logo.png') %>";
  </script> 
<% end %>


<div id="root">React is broke :(</div>
